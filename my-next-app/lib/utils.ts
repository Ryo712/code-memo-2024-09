// lib/utils.ts
export function calculateSum(a: number, b: number): number {
    return a + b;
  }
  
  export function calculateDifference(a: number, b: number): number {
    return a - b;
  }
/*このファイルにはReactの要素は含まれず、純粋なTypeScriptのロジックを書きます。単に数値を足し算や引き算する関数が定義されています。
この関数をReactコンポーネント内で使うことができます。数値を計算するための関数が含まれています。
JavaScriptのロジックや関数をTypeScriptで記述しています。ここでは、数値を計算するための関数が含まれています。*/


  